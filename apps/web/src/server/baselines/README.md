# Baseline server seam

The adapter is dependency-injected and currently exposes an in-memory repository for keyless tests.
Checkpoint integration owns durable persistence, migration, route registration, and workspace links.
Only schema-validated structured series and preference profiles cross this boundary.
